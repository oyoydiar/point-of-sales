import UserManagement from './_components/user';

export const metadata = {
  title: 'Point of Sales | User Management',
};

export default function UserManagementPage() {
  return <UserManagement />;
}
